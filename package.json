{
  "name": "before",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "gen": "prisma generate",
    "save": "yarn prisma migrate save --experimental",
    "up": "yarn prisma migrate up --experimental ",
    "start": "ts-node 'src/server.ts'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/cli": "^2.0.0-beta.5",
    "@prisma/client": "^2.0.0-beta.5",
    "ts-node": "^8.10.1"
  },
  "devDependencies": {
    "@types/node": "^14.0.1",
    "typescript": "^3.9.2"
  }
}
